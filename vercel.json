{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "pnpm install --ignore-scripts && pnpm add sharp@latest --platform=linux --arch=x64"
      }
    }
  ]
} 