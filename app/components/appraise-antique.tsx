import { useToast } from "@/components/ui/use-toast"

export default function AppraiseAntique() {
  const { toast: _toast } = useToast()
  
  // This component is intentionally empty as it's a stub
} 